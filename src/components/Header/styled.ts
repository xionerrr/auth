import styled from "styled-components";

export const StyledHeader = styled.header`
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0 50px;
    background: linear-gradient(#98989E -120% -120%, transparent);
    h2 {
        color: #98989E;
        font-size: 32px;
        cursor: default;
    }
`